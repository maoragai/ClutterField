"""Neural field architectures for predictive radar clutter modeling.

This subpackage will hold the recursive predictive neural field models
(e.g. lightweight MLPs over encoded coordinates) that learn persistent
latent scene memory and predict expected radar clutter returns.
"""

from __future__ import annotations
